{
  "$comment": "Global named-style registry for formatted fields (ADR 0014). Each style becomes one CSS class: .anti-style-<name>. Component schemas opt in per field via leantext.styles. CSS values should reference design tokens (palette vars respond to data-palette).",
  "styles": {
    "highlight": {
      "label": "Highlight",
      "css": {
        "background": "var(--palette-soft-contrast, var(--palette-surface, #ffffff))",
        "color": "var(--palette-hard-contrast, #0f172a)",
        "border-radius": "var(--radius-s, 0.25rem)",
        "padding": "0 0.15em"
      }
    },
    "accent": {
      "label": "Accent",
      "css": {
        "color": "var(--palette-accent, var(--palette-hard-contrast, #0f172a))",
        "font-weight": "var(--font-weight-medium)"
      }
    }
  }
}
